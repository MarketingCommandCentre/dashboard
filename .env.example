# Base URL of the Spring Boot backend (no trailing slash).
VITE_API_URL=http://localhost:8080

# Set to "true" to bypass the Discord login screen during local development.
# When enabled the app renders the dashboard without an authenticated user.
VITE_DEV_NO_AUTH=false
